<div class="flex justify-center items-center gap-8 container p-10">
    <div class="w-2/3">
        <iframe class="w-full"
                src="https://www.youtube.com/embed/{{ video.youtubeId }}"
                frameborder="0"
                allowfullscreen
                height="400"></iframe>
    </div>
    <div class="w-1/3">
        <div class="mb-4">
            <p class="text-xl font-semibold">{{ video.title }}</p>
            <p class="text-gray-500">Publicēts: {{ video.createdAt|date('Y-m-d H:i') }}</p>
        </div>
        <div>
            <p class="text-gray-700">{{ video.description }}</p>
        </div>
    </div>
</div>
<div class="flex gap-4 p-10">
    {% for video in relatedVideos %}
        {{ include('partials/video-card.html.twig', { video: video }) }}
    {% endfor %}
</div>